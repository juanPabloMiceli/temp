class SimulationProperties:
    
    def __init__(self, fps, forward_speed, backward_speed, rotation_speed):
        self.fps = fps
        self.forward_speed = forward_speed
        self.backward_speed = backward_speed
        self.rotation_speed = rotation_speed
